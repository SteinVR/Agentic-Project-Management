# Skill Hierarchy — Иерархия скиллов

Скиллы организованы в двухуровневую иерархию: **high-level** и **low-level**.

## High-level skill

Описывает роль, философию, ограничения и общий порядок действий —
отвечает на вопросы **ЧТО** делать и **В КАКОМ ПОРЯДКЕ**.
Не вдаётся в детали реализации подпроцессов, но явно на них ссылается
через `@skill_name`, делегируя им конкретную экспертизу.

Примеры: `dev`, `pr_review`, `onboarding`

## Low-level skill

Атомарная, переиспользуемая инструкция для одного конкретного процесса.
Отвечает на вопрос **КАК** — исчерпывающе, с нюансами, чеклистами
и edge cases. Самодостаточен: работает без контекста вызывающего скилла.
Один low-level скилл может входить в несколько high-level скиллов.

Примеры: `code_review`, `refactoring`, `testing`

## Ключевые принципы

- High-level не описывает детали — только факт делегирования (`@skill`).
- Low-level не содержит ссылок на другие скиллы — структура плоская.
- Routing между скиллами — ответственность внешнего механизма (Codex / OpenCode CLI),
  не самих скиллов.
